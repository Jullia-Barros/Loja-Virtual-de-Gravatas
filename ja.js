//function scrollHorizontally(element) {
    // Duplicar o conteúdo para permitir rolagem infinita
    //const originalContent = element.innerHTML;
    //element.innerHTML += originalContent;

   // let isDragging = false;
    //let startX;
    //let scrollLeft;

    //element.addEventListener('mousedown', (e) => {
       // isDragging = true;
       // startX = e.pageX - element.offsetLeft;
       // scrollLeft = element.scrollLeft;
       // element.style.cursor = 'grabbing'; // Muda o cursor para indicar que está arrastando
  //  });

  //  element.addEventListener('mouseleave', () => {
      //  isDragging = false;
      //  element.style.cursor = 'grab'; // Restaura o cursor
   // });

    //element.addEventListener('mouseup', () => {
      //  isDragging = false;
      //  element.style.cursor = 'grab'; // Restaura o cursor
   // });

   // element.addEventListener('mousemove', (e) => {
      //  if (!isDragging) return; // Se não estiver arrastando, sai da função
       // e.preventDefault();
       // const x = e.pageX - element.offsetLeft;
       // const walk = (x - startX) * 2; // Aumente o multiplicador para um movimento mais rápido
       // element.scrollLeft = scrollLeft - walk; // Move a rolagem
   // });
///}

//document.addEventListener("DOMContentLoaded", () => {
    //const sedaSection = document.querySelector("#GravatasSeda .scroll-container");
   // const poliesterSection = document.querySelector("#GravatasPoliester .scroll-container");

   // if (sedaSection && poliesterSection) {
   //     scrollHorizontally(sedaSection);
    //    scrollHorizontally(poliesterSection);
   // }
//});
